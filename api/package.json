{
  "name": "jeongdamgudo-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute jeongdamgudo-db --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute jeongdamgudo-db --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
